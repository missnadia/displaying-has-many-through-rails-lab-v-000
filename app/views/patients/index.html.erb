<ul>
  <% @patients.each do |patient| %>
    <li>
      Name: <%= link_to patient.name, patient_path(patient) %>
      <%= pluralize(patient.appointment_count, "appointment") %>
    </li>
  <% end %>
</ul>

<ul>
-  <% @patients.each do |patient| %>
-    <li>Name: <%= link_to patient.name, patient_path(patient) %> Number of Appointments: <%= patient.appointments_count %></li>
-  <% end %>
-</ul>
